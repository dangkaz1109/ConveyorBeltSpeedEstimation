# Conveyor Speed Estimation (RAFT + Depth)

Dự án ước lượng tốc độ di chuyển băng tải trong video bằng cách sử dụng RAFT Optical Flow và mô hình độ sâu (Depth Estimation) định dạng ONNX.

## Cài đặt

1. Clone repository về máy.
2. Cài đặt thư viện:
   ```bash
   pip install -r requirements.txt